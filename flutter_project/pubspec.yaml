name: flutter_project
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
  
    sdk: flutter
  cupertino_icons: ^1.0.8
  uuid: ^4.3.3 # تم إضافة حزمة uuid
  intl: ^0.20.2
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1
  firebase_auth: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # تم نقلها إلى dev_dependencies
  
# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # تم إضافة الأصول هنا وتفعيل مجلد الصور
